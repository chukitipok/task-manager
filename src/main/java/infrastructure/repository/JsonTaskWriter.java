package infrastructure.repository;

import core.task.Task;
import core.ports.TaskWriter;

public class JsonTaskWriter implements TaskWriter {
    public Task save(Task task) {
        return null;
    }
}
